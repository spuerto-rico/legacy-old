import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    flex: 1
  },
  bottomContainer: {
    position: 'absolute',
    bottom: 20,
    left: 10,
    right: 10,
    justifyContent: 'center',
    alignItems: 'center'
  }
});
